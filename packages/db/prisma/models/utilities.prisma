enum FeedbackType {
    Bug
    Suggestions
    General
}

model Feedback {
    id      String       @id @default(cuid())
    name    String
    email   String?
    type    FeedbackType
    rating  Int
    message String
}

// model NewsletterSubscription {
//     id         String  @id @default(cuid())
//     email      String  @unique
//     subscribed Boolean
// }
